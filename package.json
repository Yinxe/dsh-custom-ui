{
  "name": "@dshp-inx/theme-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Theme gallery for DeepSeek Harness: registers 8 open-design themes (OpenCode / Linear / Notion / Claude / NVIDIA / GitHub) with ~110 full token mappings each, plus a settings page for one-click theme switching.",
  "license": "MIT",
  "author": "Yinxe",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "default": "./lib/index.js"
    },
    "./client": {
      "default": "./client.js"
    },
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "files": [
    "lib",
    "client.js",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "dsh",
    "deepseek-harness",
    "cordis",
    "plugin",
    "theme",
    "design-system"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --check lib/index.js && node --check client.js && node --check lib/themes/opencode.js && node --check lib/themes/linear.js && node --check lib/themes/notion.js && node --check lib/themes/claude.js && node --check lib/themes/nvidia.js && node --check lib/themes/github.js && node --check lib/themes/shared.js && echo \"syntax ok\""
  }
}
